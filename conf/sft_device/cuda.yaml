# @package _global_
# CUDA / HPC profile (default on GPU nodes).
model:
  fp16: false
  bf16: true

training:
  max_grad_norm: 1.0
  train_subset_size: null

evaluation:
  force_cpu: false
  sample_size: 200
